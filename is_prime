def is_prime(number):
   
    
        
    for i in range(2, number):
        if number % i == 0:
            return False
    return True

if is_prime(number) :
    print(" ce nombre est   premier")
else:
    print("ce nombre n'est pas  premier") 
